const admin = require('firebase-admin');
const serviceAccount = require('./service-account.json'); // Wait, we don't have service account here. We can use a client script or just check local storage if we can.

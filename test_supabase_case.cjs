require('dotenv').config({ path: '.env.example' }); // using anon key
// we need an actual env to run it, but we can't.
// Wait, I can just check if any other part of the codebase uses "fullName" string exactly.
